<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="http://www.ehcache.org/v3"
        xsi:schemaLocation="http://www.ehcache.org/v3 http://www.ehcache.org/schema/ehcache-core-3.0.xsd">

    <cache alias="organisaatiot">
        <expiry>
            <ttl unit="seconds">600</ttl>
        </expiry>

        <resources>
            <heap unit="entries">10000</heap>
        </resources>
    </cache>

    <cache alias="koulutustoimijat">
        <expiry>
            <ttl unit="seconds">86400</ttl>
        </expiry>

        <resources>
            <heap unit="entries">1</heap>
        </resources>
    </cache>

    <cache alias="kayttajat">
        <expiry>
            <ttl unit="seconds">3600</ttl>
        </expiry>

        <resources>
            <heap unit="entries">10000</heap>
        </resources>
    </cache>

    <cache alias="kayttajatiedot">
        <expiry>
            <ttl unit="seconds">360000</ttl>
        </expiry>

        <resources>
            <heap unit="entries">10000</heap>
        </resources>
    </cache>

    <cache alias="perusteet">
        <expiry>
            <ttl unit="seconds">3600</ttl>
        </expiry>

        <resources>
            <heap unit="entries">10</heap>
        </resources>
    </cache>

    <cache alias="perusteKaikki">
        <expiry>
            <ttl unit="seconds">43200</ttl>
        </expiry>

        <resources>
            <heap unit="entries">50</heap>
        </resources>
    </cache>

    <cache alias="koodistohaku">
        <expiry>
            <ttl unit="seconds">60</ttl>
        </expiry>

        <resources>
            <heap unit="entries">100</heap>
        </resources>
    </cache>

    <cache alias="koodistokoodit">
        <expiry>
            <ttl unit="seconds">600</ttl>
        </expiry>

        <resources>
            <heap unit="entries">10000</heap>
        </resources>
    </cache>

    <cache alias="koodistot">
        <expiry>
            <ttl unit="seconds">600</ttl>
        </expiry>

        <resources>
            <heap unit="entries">100</heap>
        </resources>
    </cache>

    <cache alias="lokalisoinnit">
        <expiry>
            <ttl unit="seconds">600</ttl>
        </expiry>

        <resources>
            <heap unit="entries">10000</heap>
        </resources>
    </cache>

    <cache alias="organisaationimi">
        <expiry>
            <ttl unit="seconds">86400</ttl>
        </expiry>

        <resources>
            <heap unit="entries">1000</heap>
        </resources>
    </cache>

    <cache alias="koulutuksenjarjestajat">
        <expiry>
            <ttl unit="seconds">600</ttl>
        </expiry>

        <resources>
            <heap unit="entries">10000</heap>
        </resources>
    </cache>

    <cache alias="ops-navigation">
        <expiry>
            <ttl unit="seconds">86400</ttl>
        </expiry>

        <resources>
            <heap unit="entries">500</heap>
        </resources>
    </cache>

    <cache alias="geneerinenarviointi">
        <expiry>
            <ttl unit="seconds">7200</ttl>
        </expiry>

        <resources>
            <heap unit="entries">100</heap>
        </resources>
    </cache>

</config>